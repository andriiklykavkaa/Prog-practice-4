#include <stdio.h>

#include "string_appender.h"


int main(void) {
    char source[] = "Hell0";
    char dest[] = " world";

    // char* pDest = append_string(&dest[0], &source[0]);
    // printf("%s", pDest);
    return 0;
}
